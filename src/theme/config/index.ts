/**
 * Theme config — tokens consumed by Tailwind / NativeWind.
 *
 * Prefer className + cn() for layout. Use palette or CSS variables for inline
 * style when needed (e.g. Reanimated). Shared UI lives in src/ui; use
 * tailwind-variants when variants reduce duplication.
 */
export * from './colors'
export * from './spacing'
export * from './typography'
